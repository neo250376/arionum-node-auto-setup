# arionum-node-auto-setup
Files needed for automatic arionum node setup
This is purely a repository for holding the files need to automatically setup an arionum node with stats tracking and database dump
